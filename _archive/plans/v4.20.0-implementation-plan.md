# v4.20.0 Implementation Plan — Responsive Breakpoints + Gradient Cleanup

**Status**: IN PROGRESS
**Date**: 2026-04-17
**Backlog items**: 4X-29 (responsive breakpoints), 4X-30 (gradient cleanup)
**Release type**: Feature (4.x.0) — CSS changes across 10+ files, pre-release recommended

---

## Scope

### 4X-29: Responsive breakpoints for 9 panels
Add `@media (max-width: 30rem)` single-column fallback to all panels that currently lack it. Normalize power panel's pixel breakpoints to `rem` (Data condition).

### 4X-30: Gradient audit and cleanup
Remove 4 decorative gradients from `lcars-homepage-card.js`. Keep 10 functional gradients (documented).

## Pre-Implementation Reviews

- **Geordi (Phase 1)**: Full design spec with per-panel grid collapse rules, cylinder-to-horizontal transforms, and gradient classification (10 KEEP, 4 REMOVE).
- **Data (Phase 2)**: APPROVED WITH CONDITION — normalize power panel px→rem breakpoints. Net bundle impact ≈ 0.
- **Worf (Phase 2)**: Auto-approved (pure CSS, no security surface).

## Stories (execution order)

### Story 1: Normalize power panel breakpoints (Data condition)
**File**: `lcars-power-panel-styles.js`
**Change**: `1023px → 64rem`, `767px → 48rem`, `479px → 30rem`

### Story 2: Add responsive breakpoints to 9 panels
**Files**: alarm, battery, camera, climate, environment, irrigation, media, weather, pool-spa panel style files
**Change**: Add `@media (max-width: 30rem)` blocks per Geordi's spec
**Special**: Battery and environment panels include cylinder-to-horizontal transforms

### Story 3: Remove 4 decorative gradients from homepage card
**File**: `lcars-homepage-card.js`
**Changes**:
- L2486: Forecast range fill → flat `var(--lcars-butterscotch)`
- L3246: Waveform peak bar → flat `var(--lcars-tomato)`
- L3413: Weather glow → remove `::before` pseudo, use border-color shift
- L3521: Pool caustic shimmer → remove `::after` pseudo + `lcars-caustic-drift` keyframes

## Risk Assessment

- **LOW**: CSS-only changes, no JS logic affected
- **MEDIUM**: Battery/environment cylinder rotation could look wrong on narrow screens — needs live testing
- Pre-release recommended for visual verification

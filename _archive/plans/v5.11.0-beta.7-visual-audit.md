# LCARS Visual Audit — v5.11.0-beta.7 (Medical Bay focused review)

Capture: [screenshots/v5.11.0-beta.7/01-medical-home-top.png](../screenshots/v5.11.0-beta.7/01-medical-home-top.png) (1 image — single-view focused review at Captain's request, not a full Phase A crawl).
Audit date: 2026-05-15. Reviewer: full agent team per `.github/VISUAL-CRAWL-PROTOCOL.md`.
Bundle deployed: `LCARS 5.11.0-BETA.7` (footer-verified).

Scope: Captain shipped four iterations of Tactical Readout Tab callout work today (beta.4 → beta.7). This focused audit confirms the callout overhaul is now visually clean and surfaces any remaining items before declaring the train done.

---

## Triage (Riker)

| ID | Sev | Officer | Defect | Fix owner |
| --- | --- | --- | --- | --- |
| — | ✅ | Geordi | All four Captain-driven beta.4–beta.7 issues resolved (border clipping, sensitive-area dot, cross-tab leaders, text overflow) | none |
| MED-01 | S2 | Data | `BMI` tile renders `—` (entity not bound on this HA) | Data — verify Withings BMI entity exists; if absent, swap to derived value `weight / height²` |
| MED-02 | S2 | Data | `REGULARITY` row of SLEEP tile renders `—` (entity not bound) | Data — verify Oura sleep regularity entity; otherwise hide row |
| MED-03 | S2 | Wesley | `RESP` and `DISTANCE` callouts render dimmed-dash tabs even with no entity value — visually noisy on an otherwise-clean silhouette | Wesley + Data — decide policy: hide tab when value is null OR keep as "slot reserved" indicator |
| — | ✅ | Worf | No exposed entity IDs / IPs / tokens / stack traces; admin chrome appropriate for admin session | none |
| — | ✅ | Wesley | Filter-pill / toggle states render with clear active/inactive separation; no color bleed | none |

No S0 or S1 defects. Train is ship-ready pending Captain authorization.

---

## Geordi La Forge — chrome + pixel pass (LEAD)

### Chrome parity (was the long-standing Medical filler bug from v5.7.0-beta.2 fixed?)

✅ **Sidebar**: SICKBAY pill (purple) → BIOFUNCTION MONITOR pill (purple) → dark/neutral filler below. No solid violet block under the second pill. Bottom-left elbow + GtG account button render correctly.
✅ **Endcap**: SICKBAY orange header bar carries all three admin icons (mute, sort, gear) right-aligned. Matches LCARS-PARITY-medical-admin-chrome.

### Tactical Readout Tab callouts (the v5.11.0-beta.4 → beta.7 work)

| Check | Result |
| --- | --- |
| Tab borders never touch / overlap body silhouette outline | ✅ — visible ~12 viewBox-unit clearance maintained on every callout, regardless of label length |
| Tab text fits inside the border on every callout | ✅ — `WEIGHT 82.1`, `SPO2 96.7`, `BP 105/73`, `STEPS 505`, `ACTIVE 0` all framed cleanly with breathing room |
| Leader lines do not cross other callout tabs | ✅ — anchor-aligned slot allocation (beta.5) holds; verticals are zero-length on the current Medical config so no leader can intrude |
| Tab orientation: rounded cap at canvas edge, flat side facing body | ✅ — symmetrical mirror across the silhouette; LCARS-canonical |
| Sensitive-area policy: no callout dots on crotch / groin | ✅ — WEIGHT dot now at navel (`abdomen` y=38), well clear of the silhouette V |
| Color coding by status | ✅ — NOMINAL/sky-blue for live values, dimmed for `RESP` and `DISTANCE` (no value) |
| Anchor dot terminator on body | ✅ — small filled circle at every anchor point |

### Other pixel observations

- Header bar (`MEDICAL REPORT · 4792-614782.4`) renders correctly with three intentional reference number pills (`898798 849004 461247`) and THERM / NOMINAL toggles.
- Filter pills `SUMMARY / ANATOMICAL / BIOMEDICAL` render with active state (`SUMMARY` lighter purple) clearly distinct from inactive states.
- Tile-grid below silhouette: column widths, separators, and Antonio typography are consistent across all 12 visible tiles.

No clipping, color drift, or focus-visibility regressions observed.

---

## Data — telemetry presence and integrity

Active vital values rendered:
`HR 74 BPM`, `TEMP +0.1 °C`, `WEIGHT 82.1 KG`, `BODY FAT 16.7 %`, `FAT MASS 13.74 KG`, `LEAN 68.35 KG`, `MUSCLE 65.65 KG`, `BONE 2.70 KG`, `VISCERAL 3.3`, `HYDRATION 44.29 L`, `READINESS 88/100`, `SLEEP 85/100`, `SLEEP TIME 7H 0M`, `DEEP 0H 53M`. HR sparkline sub-rows populated (PULSE/MAX/MIN/AVG/LOW SLEEP/AVG SLEEP).

**Empty / `—` placeholders**:

| Field | Where | Likely cause |
| --- | --- | --- |
| `BMI` | Tile | Entity not bound on this HA — could be derived from existing weight/height pair |
| `REGULARITY` | Sleep tile sub-row | Oura sleep-regularity entity unconfigured |
| `RESP` | Silhouette callout | No respiration entity bound |
| `DISTANCE` | Silhouette callout | No distance entity bound |

These are integration-coverage gaps, not regressions. The dimmed-color treatment correctly distinguishes "no value" from "value present", which is exactly what the offline-color contract calls for.

---

## Worf — security / exposed surfaces

- No raw entity IDs visible (`sensor.xxx_yyy` patterns absent).
- No IP addresses, MAC addresses, tokens, or stack traces.
- `4792-614782.4` and the three smaller numeric pills are intentional LCARS reference identifiers, not telemetry leakage.
- Admin chrome (mute / sort / gear) appears in admin session — appropriate.

Clean.

---

## Wesley — affordance + interaction

- View-mode pills (`SUMMARY / ANATOMICAL / BIOMEDICAL`) have clear active/inactive separation — gold-on-violet vs dark-on-violet.
- THERM / NOMINAL toggles right-aligned, color-coded.
- Tile-grid columns are clickable affordances; their borders frame each tile distinctly without color bleed.
- Suggestion (deferred, MED-03 above): consider hiding `RESP` / `DISTANCE` callout tabs when no entity is bound, rather than rendering a dimmed-dash tab. Currently the tabs visually compete with live ones for attention.

---

## Q — non-LCARS pages

No `past.html` / `dontpanic.html` captures in this focused audit. Skip.

---

## Recommendation

**Ship v5.11.0-beta.7 → v5.11.0** at Captain's authorization. The Tactical Readout Tab callout overhaul (beta.4 → beta.7) is visually correct and the four Captain-flagged issues are resolved. The three S2 telemetry gaps (BMI, REGULARITY, RESP, DISTANCE) are integration-coverage items that can be addressed in a follow-up beta or the next minor.

# LCARS Illumination Dashboard — Lovelace YAML configuration
# Standalone dashboard: all lighting and circuits across all areas.
# Uses panel mode — card fills entire viewport (no LCARS sidebar).

views:
  - title: Illumination
    icon: mdi:lightbulb
    path: home
    panel: true
    cards:
      - type: custom:illumination-card

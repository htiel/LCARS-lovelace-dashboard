# LCARS Illumination Dashboard — Lovelace YAML configuration
# Standalone dashboard with own LCARS frame + 3 filter sidebar buttons.
# Uses panel mode so the card renders its own full frame.

views:
  - title: Illumination
    icon: mdi:lightbulb
    path: home
    panel: true
    cards:
      - type: custom:illumination-card

# LCARS Illumination Dashboard — Lovelace YAML configuration
# Uses lcars-illumination-layout: LCARS frame with 3 filter sidebar buttons.

views:
  - title: Illumination
    icon: mdi:lightbulb
    path: home
    type: custom:lcars-illumination-layout
    cards:
      - type: custom:illumination-card

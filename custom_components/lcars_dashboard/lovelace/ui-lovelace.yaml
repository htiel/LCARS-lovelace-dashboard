# LCARS Dashboard — Lovelace YAML configuration
# Non-standard top-level keys (lcars_dashboard, button_card_templates,
# apexcharts_card_templates, lovelace-background) were removed because
# HA 2025.x+ Lovelace config validation rejects unknown keys, causing
# "CONFIGURATION ERROR" banner.

views:
  !include_dir_merge_list views/

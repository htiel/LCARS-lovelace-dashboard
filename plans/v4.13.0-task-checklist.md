# LCARS v4.13.0 — Task Checklist

> Flat checklist with spec references. Every v4.13.0 enhancement = one checkbox.  
> Group by phase. Mark as ✅ when implemented, tested, and reviewed.

---

## Phase 0 — Shared Framework

- [x] **0.1** Create `lcars-shared-animations.js` — shared keyframes module  
  Spec: V413-BRIEF §DRY Recovery, DEVICE-PANEL §7.6–7.10  
  File: `js/src/lcars-shared-animations.js` (NEW)  
  Exports: `viewscreen-activate`, `cascade-in`, `lcars-distress-pulse`, `lcars-frame-breathe`, `lcars-button-flash`, `lcars-scanline`, reduced-motion overrides

- [x] **0.2** Update `lcars-color-utils.js` — STATE_COLOR_MAP + CO₂ 3-tier  
  Spec: AIR-PURIFIER-VERIFY §v4.13 enh.2, V413-BRIEF §DRY Recovery, TEMP-GRID §5–6  
  File: `js/src/lcars-color-utils.js` (EDIT)  
  Changes: Reconcile `getCo2Color()` to 3-tier, add `STATE_COLOR_MAP`, add `getTempColor()`, `getHumidityColor()`, `getComfortColor()`, `COMFORT_COLORS` whitelist

- [x] **0.3** Update `lcars-styles.js` — animation timing tokens  
  Spec: Data R-5, UI-ARCHITECTURE  
  File: `js/src/lcars-styles.js` (EDIT)  
  Changes: Add `--lcars-anim-breathe`, `--lcars-anim-transition`, `--lcars-anim-slow`, `--lcars-anim-confirm`, `--lcars-anim-scan`, `--lcars-anim-stagger`

---

## Phase 1 — Device Panel Base (cascades to ALL panels)

- [x] **1.1** Frame Breathing Pulse — 4s opacity cycle on panel border  
  Spec: DEVICE-PANEL §7.6, V413-BRIEF §1.1  
  CSS: `@keyframes lcars-frame-breathe` opacity 0.88→1.0→0.88  
  Target: `.lcars-device-panel`

- [x] **1.2** Data Pip Footer Strip — 4px decorative segment strip  
  Spec: DEVICE-PANEL §7.7, V413-BRIEF §1.2  
  CSS: `::after` pseudo-element, `repeating-linear-gradient`  
  Target: `.lcars-device-panel::after`

- [x] **1.3** Header Numeric Code Watermark — faint 6-digit code  
  Spec: DEVICE-PANEL §7.8, V413-BRIEF §1.3  
  Conditions: Worf M3 (`<span aria-hidden>`), Data L-4 (`hash % 1000000`)  
  Target: Panel header, right-aligned

- [x] **1.4** Button Press Ripple Flash — 250ms radial flash on press  
  Spec: DEVICE-PANEL §7.9, V413-BRIEF §1.4  
  CSS: `@keyframes lcars-button-flash` scale+opacity on `:active::after`  
  Target: All LCARS pill buttons

- [x] **1.5** Viewscreen Power-On Scanline — 600ms horizontal sweep  
  Spec: DEVICE-PANEL §7.10, V413-BRIEF §1.5  
  CSS: `@keyframes lcars-scanline` translateY on `::before`  
  Target: `.lcars-media-viewscreen::before`, single fire on firstUpdated

---

## Phase 2 — Battery Panel

- [x] **2.1** Sensor Pill Badges — label+value capsule format  
  Spec: V413-BRIEF §2.1  
  CSS: `display: inline-flex` pill with `.pill-label` + `.pill-value`  
  Animation: 300ms gold flash on value update

- [x] **2.2** Slider Snap Detent Feedback — 10% boundary glow pulse  
  Spec: V413-BRIEF §2.2  
  CSS: `@keyframes lcars-detent-pulse` 150ms box-shadow  
  JS: Detect boundary crossing in `input` event

- [x] **2.3** Charge State Glow Aura — colored box-shadow on % readout  
  Spec: V413-BRIEF §2.3  
  CSS: `box-shadow` with color by charge state (ice/golden-orange/tomato)  
  Animation: Static glow, `transition: 500ms`

- [x] **2.4** Segment Bar Meter — 10-segment discrete battery bar  
  Spec: V413-BRIEF §2.4, DEVICE-PANEL §13 (warp core: 7-tier center-out)  
  CSS: `display: flex; gap: 2px` with 10 `<span>` segments  
  Animation: `opacity 300ms` on segment state change

---

## Phase 3 — Climate Panel

- [x] **3.1** Arc Gauge Ring Segments — stroke-dasharray segmented arc  
  Spec: CLIMATE §13 enh.1  
  CSS: `stroke-dasharray: 6 2`, `transition: stroke-dashoffset 800ms`  
  Animation: Leading segment gold flash 400ms on setpoint change

- [x] **3.2** HVAC Action Frame Pulse — 2s border oscillation  
  Spec: CLIMATE §13 enh.2  
  CSS: `@keyframes lcars-hvac-pulse` 2s, `--pulse-color` per action  
  Condition: Data M-4 — `color-mix()` fallback with hardcoded `rgba()`

- [x] **3.3** Setpoint Button Glow Feedback — 400ms scale+text-shadow  
  Spec: CLIMATE §13 enh.3  
  CSS: `@keyframes lcars-setpoint-confirm` scale(1.05) + gold text-shadow  
  Target: `.lcars-target-temp.confirm`

- [x] **3.4** Mode Strip Active Indicator — traveling 2px gold bar  
  Spec: CLIMATE §13 enh.4  
  CSS: `::after` on `.lcars-mode-strip`, `transition: transform 300ms`  
  JS: Calculate `--indicator-x` and `--indicator-width`

- [x] **3.5** Ambient Temperature Data Pips — 24-hour micro-heatmap  
  Spec: CLIMATE §13 enh.5  
  CSS: 24 × 4px squares, colored by `getTempColor()`  
  Data: HA history API for 24h temperature

---

## Phase 4 — Media Card

- [x] **4.1** Audio Waveform Visualizer — 12-bar EQ animation  
  Spec: MEDIA v4.13.0, V413-BRIEF §3.1  
  Conditions: Data C-1 (scaleY not height), Data C-2 (12 bars, 4 timing groups)  
  CSS: `@keyframes lcars-waveform` scaleY(0.2→1.0), active during playback only

- [x] **4.2** Album Art Viewscreen Border Glow — 3s box-shadow pulse  
  Spec: MEDIA v4.13.0  
  CSS: `box-shadow: 0 0 12px 4px var(--lcars-african-violet)` + 3s pulse  
  Target: Media viewscreen frame, playing state only

- [x] **4.3** Transport Button Active States — glow ring + indicator dot  
  Spec: MEDIA v4.13.0  
  CSS: `box-shadow` on `.active` + `::before` colored dot

- [x] **4.4** Progress Bar Luminous Head — 4px pip with 2s glow  
  Spec: MEDIA v4.13.0  
  CSS: `::after` on `.media-progress-fill`, `box-shadow` pulse 2s

- [x] **4.5** Idle State Standby Pulse — 4s ♪ glyph breathing  
  Spec: MEDIA v4.13.0, MEDIA §5  
  CSS: `@keyframes` opacity 4s on idle music note

---

## Phase 5 — Alarm Panel

- [x] **5.1** Red Alert Frame Strobe — 1s tomato pulse + 20px glow  
  Spec: ALARM §8 v4.13.0 enh.1  
  CSS: `@keyframes lcars-red-alert` 1s linear infinite  
  Condition: Data C-3 — §8.1–8.10 LEGACY, use v4.13.0 only

- [x] **5.2** Shield Icon Reactive Glow — 4 states, filter:drop-shadow  
  Spec: ALARM §8 v4.13.0 enh.2  
  Condition: Worf M1 — triggered ≥0.5s (2 Hz max)  
  States: disarmed=ice, home=butterscotch, away=3s pulse, triggered=0.5s critical

- [x] **5.3** Keypad Button Tactile Flash — ghost digit float-up  
  Spec: ALARM §8 v4.13.0 enh.3  
  CSS: `@keyframes lcars-key-preview` 200ms translateY + opacity  
  Target: `.lcars-keypad-btn:active::before` using `attr(data-digit)`

- [x] **5.4** Countdown Timer Urgency Escalation — 4-tier system  
  Spec: ALARM §8 v4.13.0 enh.4  
  Tiers: calm (>15s), elevated (10–15s), high (5–10s), critical (<5s)  
  CSS: `[data-urgency]` attribute, color + animation escalation

- [x] **5.5** Zone Status Micro-Pips — 6px state dots per zone  
  Spec: ALARM §8 v4.13.0 enh.5  
  CSS: `::before` on `.lcars-zone-row`, `@keyframes lcars-pip-flash` 300ms  
  Colors: ice=OK, butterscotch=bypass, tomato=fault

---

## Phase 6 — Weather Panel

- [x] **6.1** Condition-Reactive Ambient Glow — 9-condition radial gradient  
  Spec: WEATHER §9 v4.13.0 enh.1  
  CSS: `::before` on viewscreen, 9 `:host([condition])` selectors  
  Condition: Worf M2 — storm flicker 4s (not 3s)

- [x] **6.2** Wind Compass Animated Needle — 800ms rotation + gusty  
  Spec: WEATHER §9 v4.13.0 enh.2  
  CSS: `transition: transform 800ms`, `.gusty` → `lcars-gust-oscillate` ±5°  
  JS: Normalize `--wind-deg` for shortest rotation path

- [x] **6.3** Forecast Strip Temperature Range Bars — staggered scaleY grow  
  Spec: WEATHER §9 v4.13.0 enh.3  
  CSS: `@keyframes lcars-bar-grow` 400ms, `--day-index` × 60ms stagger  
  Gradient: ice→sunflower→butterscotch (approved data-viz exception)

- [x] **6.4** Sun Arc Day Progress — SVG semicircle with gold tracking dot  
  Spec: WEATHER §9 v4.13.0 enh.4  
  CSS: `stroke-dashoffset` transition 60s, dot `cx`/`cy` transition 60s  
  Entity: `sun.sun` for rise/set times

- [x] **6.5** Precipitation Probability Pips — 10 pips in 5×2 grid  
  Spec: WEATHER §9 v4.13.0 enh.5  
  CSS: 4px×4px pips, filled = `--lcars-ice`, empty = gray 30%  
  JS: `Math.round(precip_probability / 10)` filled pips, 30ms stagger

---

## Phase 7 — Pool/Spa Panel

- [x] **7.1** Water Body Caustic Shimmer — 3-layer radial gradient drift  
  Spec: POOL-SPA §8 v4.13.0 enh.1  
  CSS: `@keyframes lcars-caustic-drift` 12s, `::after` 200%×200%, opacity 0.06  
  Blend: `mix-blend-mode: screen`

- [x] **7.2** Heating Active Indicator — 3px animated gradient energy bar  
  Spec: POOL-SPA §8 v4.13.0 enh.2  
  CSS: `@keyframes lcars-heat-flow` 2s, tomato→golden-orange→butterscotch  
  Off state: static dim gray

- [x] **7.3** Chemistry Sensor Alert Thresholds — pill badges with thresholds  
  Spec: POOL-SPA §8 v4.13.0 enh.3  
  CSS: `[data-threshold]` ok/warn/critical, `@keyframes lcars-chem-alert` 1.5s  
  Condition: Data C-4 — worst-threshold drives pulse

- [x] **7.4** IntelliBrite Color Swatch Glow — active swatch halo  
  Spec: POOL-SPA §8 v4.13.0 enh.4  
  CSS: `box-shadow: 0 0 8px 2px var(--swatch-color)`, `transition: 300ms`  
  Behavior: Glow transfers between swatches on mode change

- [x] **7.5** Pump Status Running Indicator — 3-dot chase spinner  
  Spec: POOL-SPA §8 v4.13.0 enh.5  
  Condition: Data R-6 — primary pump only  
  CSS: `@keyframes lcars-pump-spin` 1.2s, staggered 120° offsets

---

## Phase 8 — Irrigation Panel

- [x] **8.1** Barberpole Flow Animation — diagonal stripe scroll  
  Spec: IRRIGATION §10 v4.13.0 enh.1  
  CSS: `@keyframes lcars-flow` 0.6s, `repeating-linear-gradient(-45deg, ...)`  
  Target: `.lcars-zone-fillbar.active .fill`, 11.31px period

- [x] **8.2** Zone Completion Flash — 2s green-to-dark confirmation  
  Spec: IRRIGATION §10 v4.13.0 enh.2  
  CSS: `@keyframes lcars-zone-complete` 2s, `.completing` class  
  Trigger: `watering → idle` transition, `animationend` cleanup

- [x] **8.3** Schedule Countdown Proximity Glow — text-shadow ramp  
  Spec: IRRIGATION §10 v4.13.0 enh.3  
  CSS: `text-shadow: calc(--schedule-proximity * 8px)`, T-0 flash 500ms  
  JS: `--schedule-proximity` updated every 60s  
  Condition: Worf M4 — `Number()` + `!isNaN()` guard

- [x] **8.4** Rain Delay Cloud Badge — bobbing pill with ☁  
  Spec: IRRIGATION §10 v4.13.0 enh.4  
  CSS: `@keyframes lcars-cloud-bob` ±1px, 3s  
  Visibility: Only when `rain_delay > 0`

---

## Phase 9 — Atmoscrubber Panel

- [x] **9.1** Enhanced Particle Drift — variable 6-particle system  
  Spec: ATMOSCRUBBER v4.13.0, V413-BRIEF §9.1  
  Conditions: Data C-5 (6 max), Data R-4 (single merged keyframe)  
  CSS: `@keyframes lcars-particle-float` with per-particle CSS vars

- [x] **9.2** AQI Cylinder Ambient Glow — inset box-shadow by AQI  
  Spec: ATMOSCRUBBER v4.13.0, V413-BRIEF §9.2  
  CSS: `box-shadow: inset 0 0 12px 4px`, `transition: 1s`  
  Unhealthy: 2s intensity pulse

- [x] **9.3** Filter Life Segment Bar — 10 discrete segments  
  Spec: ATMOSCRUBBER v4.13.0, V413-BRIEF §9.3  
  Thresholds: <30% golden-orange, <10% tomato+pulse  
  CSS: `@keyframes lcars-filter-critical` 1s opacity

- [x] **9.4** Sparkline Scan Animation — stroke-dashoffset draw-on  
  Spec: ATMOSCRUBBER v4.13.0, V413-BRIEF §9.4  
  Condition: Data L-5 — `getTotalLength()` in firstUpdated/updated only  
  CSS: 1.5s draw, `--sparkline-index` × 200ms stagger

- [x] **9.5** Preset Mode Transition Wipe — directional fill  
  Spec: ATMOSCRUBBER v4.13.0, V413-BRIEF §9.5  
  CSS: `::before` pseudo-element, `transition: width 250ms`

---

## Phase 10 — Air Purifier Verification (BlueAir)

- [x] **10.1** Sensor Row Stagger — 7-row cascade-in  
  Spec: AIR-PURIFIER-VERIFY §v4.13 enh.1  
  CSS: `@keyframes lcars-sensor-row-in` 250ms, `--sensor-index` × 80ms  
  730ms total for 7 rows

- [x] **10.2** CO₂ Threshold Color Mapping — 3-tier reconciliation  
  Spec: AIR-PURIFIER-VERIFY §v4.13 enh.2  
  Depends: Phase 0.2 (`getCo2Color()` reconciliation)  
  Tiers: ice ≤800, sunflower 801–1200, tomato >1200

- [x] **10.3** Multi-Sparkline Stagger — 4 metric sparklines  
  Spec: AIR-PURIFIER-VERIFY §v4.13 enh.3  
  Colors: PM2.5=ice, PM1=sunflower, CO₂=almond, VOC=lilac  
  Stagger: `--sparkline-index` × 200ms

- [x] **10.4** Filter Expired Flash — 600ms single-fire tomato  
  Spec: AIR-PURIFIER-VERIFY §v4.13 enh.4  
  CSS: `@keyframes lcars-filter-expired` 600ms box-shadow  
  Trigger: `binary_sensor.*_filter_expired` → `on`  
  Cleanup: `{ once: true }` animationend listener

---

## Phase 11 — Temperature/Humidity Grid

- [x] **11.1** Tile Comfort Glow — ambient box-shadow by comfort zone  
  Spec: TEMP-GRID §12 enh.1  
  CSS: `@keyframes lcars-warm-glow / lcars-cool-glow` 3s  
  Condition: Worf R1 — `COMFORT_COLORS` whitelist enforced

- [x] **11.2** Floor Label Scan-In — horizontal wipe reveal  
  Spec: TEMP-GRID §12 enh.2  
  CSS: `::after` overlay, `@keyframes lcars-floor-scan` 200ms  
  Stagger: `--floor-index` × 200ms

- [x] **11.3** Sparkline Draw-On — batched stroke-dashoffset  
  Spec: TEMP-GRID §12 enh.3  
  CSS: 1.2s draw, `--tile-index` × 50ms stagger  
  Condition: Data L-5 — getTotalLength in firstUpdated; batch reads before writes

- [x] **11.4** Summary Row Pulse — 4s border breathing  
  Spec: TEMP-GRID §12 enh.4  
  CSS: `@keyframes lcars-summary-pulse` 4s border + box-shadow

- [x] **11.5** Hot/Cold Alert Tile Pulse — asymmetric urgency  
  Spec: TEMP-GRID §12 enh.5  
  CSS: Hot 1.5s (peach↔tomato), Cold 2s (bluey↔blue)  
  Rule: Moderate deviations glow only, extreme deviations pulse + glow

- [x] **11.6** Value Change Ripple — 300ms border-left accent  
  Spec: TEMP-GRID §12 enh.6  
  CSS: `@keyframes lcars-value-ripple` 300ms border-width  
  JS: `--tile-new-comfort-color`, force reflow restart pattern

---

## Phase 12 — Integration & Verification

- [x] **12.1** Build verification — `npm run build` succeeds, bundle ≤ 290 KiB
- [x] **12.2** Reduced motion audit — all ambient disabled, confirmations halved
- [x] **12.3** Animation budget audit — ≤6 concurrent per panel, ≤2 box-shadow keyframes
- [x] **12.4** Review conditions — all 19 Worf/Data conditions verified (see plan tracker)
- [x] **12.5** Cross-browser — Chrome ≥111, Safari ≥16.2, Firefox ≥113, `color-mix()` fallback

---

## Summary

| Phase | Tasks | Status |
|-------|-------|--------|
| 0 — Shared Framework | 3 | ✅ |
| 1 — Device Base | 5 | ✅ |
| 2 — Battery | 4 | ✅ |
| 3 — Climate | 5 | ✅ |
| 4 — Media | 5 | ✅ |
| 5 — Alarm | 5 | ✅ |
| 6 — Weather | 5 | ✅ |
| 7 — Pool/Spa | 5 | ✅ |
| 8 — Irrigation | 4 | ✅ |
| 9 — Atmoscrubber | 5 | ✅ |
| 10 — BlueAir | 4 | ✅ |
| 11 — Temp Grid | 6 | ✅ |
| 12 — Integration | 5 | ✅ |
| **TOTAL** | **61** | **ALL SHIPPED — v4.13.0 / v4.13.1** |

# v4.18.9 Implementation Plan — Bug Fixes

**Status**: COMPLETE — shipped v4.18.9
**Date**: 2026-04-17
**Backlog items**: 4X-27, 4X-31, 4X-32, 4X-34

---

## Scope

Four quick-win bug fixes from the top of the WSJF-ordered backlog. All are patches to existing panels and backend — no new panels, no new components.

## Stories

### Story 1: 4X-27 — Clean up hass.data[DOMAIN] (XS)
**File**: `custom_components/lcars_dashboard/__init__.py`
**Change**: `async_unload_entry` now calls `hass.data.pop(DOMAIN, None)` after successful platform unload. Returns `unload_ok` instead of hardcoded `True`.
**Review**: Data APPROVE, Worf APPROVE (security improvement — cleans stale data).

### Story 2: 4X-34 — Replace legacy 'D' icon (XS)
**File**: `custom_components/lcars_dashboard/load_dashboard.py`
**Change**: Default sidebar icon changed from `mdi:alpha-d-box` to `mdi:star-four-points`. Consistent with config_flow default.
**Review**: Geordi APPROVE (better thematic fit).

### Story 3: 4X-32 — Switches in Illumination Circuits (S)
**File**: `custom_components/lcars_dashboard/js/src/panels/illumination/lcars-illumination-panel.js`
**Change**: Pass 2 of `_partitionLightingEntities()` now includes all `switch` domain entities (excluding `device_class: outlet`) in Circuits. Data flagged outlet exclusion bypass — applied.
**Review**: Data APPROVE WITH CONDITIONS (outlet guard added), Geordi APPROVE.

### Story 4: 4X-31 — Life Support panel clipping (M)
**Files**: `lcars-lifesupport-panel-styles.js`, `lcars-panel-frame-styles.js`
**Change**: Added `overflow: visible` to `.ls-content`, `.ls-substations`, `.ls-substation`, `.panel-content`.
**Review**: Geordi APPROVE. **NOTE: Fix was insufficient — clipping persists. Reopened as 4X-46 (#44).**

## Lessons Learned

- **Phases 1-3 were skipped.** Implementation started without design review or plan. This contributed to the wrong fix for 4X-31.
- **Geordi should have reviewed the clipping root cause in Phase 1**, not just the CSS fix in Phase 6.
- Implementation plans must be created BEFORE code is written, not retroactively.
